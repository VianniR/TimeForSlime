using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DragCollider : MonoBehaviour
{
    public PlayerController player;

    void OnMouseDown()
    {
        /*if (player.onGround && !player.isHoldingMouse && !player.isDead)
        {
            player.jumpIndicator.gameObject.SetActive(true);
            player.isHoldingMouse = true;
        }*/
    }
}
